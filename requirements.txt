# Python dependencies for the lead-paint-housing-brief pipeline.
#
# Tested with Python 3.10+. Install:
#     python3 -m pip install -r requirements.txt
#
# Also requires the system binary `pdftotext` (poppler):
#     brew install poppler          # macOS
#     apt-get install poppler-utils # Debian/Ubuntu

requests>=2.31,<3
lxml>=5.0,<6
matplotlib>=3.7,<4
plotly>=6.0,<7
kaleido>=0.2,<1
